  alert('Hello world!');


var MyArray =(1,2, "3")


var myFaves= {
    pet: "cat",
    drink: "chocholate milk",
    number: 1,
    array: [1,2,3],
    list: myFaves,  
    };

    const name = prompt("Shay");

const checkName = function(prompt) {
    var name = prompt("What is your name?");
  if (name === "") {
    name = prompt("What is your name, for real this time?");
    checkName(); // repeats until a name exists
  } 
alert("hello, ")
};

checkName(); // kicks off the name-checking the first time

alert("Hello " + Mikela);